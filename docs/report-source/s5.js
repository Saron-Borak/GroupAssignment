const L = require('./lib.js');
const { P, Rich, H1, H2, H3, Bullet, Num, Code, Tbl, TableCaption, FigCaption } = L;

module.exports = [
  H1('5. Testing'),

  H2('5.1 Strategy'),
  P('Testing was performed at the three levels named in the brief, plus a simulated acceptance pass.'),
  Tbl(['Level', 'What was tested', 'How'], [
    ['Unit', 'The rules the three modules share, held on the backed enumerations: what counts as attendance, which complaint states are open, where each role lands after sign-in.', 'Assertions against the enumeration methods directly, with no database.'],
    ['Integration', 'That the three subsystems resolve a student through the shared profile and return consistent figures.', 'Records created in all three modules for one student, then read back through the insight service.'],
    ['System', 'Every screen in all three portals, and the write path of each module end to end: creating and marking a register, issuing and submitting an assignment, filing and answering a complaint.', 'Requests through the real routing stack, so middleware, policies, validation, services and the database are exercised together.'],
    ['Acceptance', 'The full demonstration path a marker would follow.', 'Manual walkthrough in a browser against the seeded database.'],
  ], [1500, 5000, 4300]),
  TableCaption('Testing levels'),
  P('The suite runs against an in-memory SQLite database rebuilt for each test, so tests are fast and cannot contaminate the demonstration data. Lazy loading is disabled during testing, so any relationship the code failed to load in advance raises an exception rather than quietly issuing an extra query per row: a performance regression fails the build.'),

  H2('5.2 Automated Test Suite'),
  Tbl(['Test class', 'Tests', 'Coverage'], [
    ['StudentProfileTest', '12', 'Create, update, archive, restore; duplicate detection; age, telephone and normalisation rules; repeat rows'],
    ['PhotoUploadTest', '5', 'Valid upload, wrong type, oversize, replacement cleanup, removal'],
    ['ModuleIntegrationTest', '9', 'Attendance percentage and excused handling; submission and complaint counts; combined profile; cohort query count; archive retains history'],
    ['ModuleWorkflowTest', '31', 'Marking and re-marking a register; roster filtering; session clashes; issuing, amending and withdrawing an assignment; submitting, late detection, replacement, executable rejection; filing a case, references, registry response, reopening, withdrawal, the summary report and its notification'],
    ['CheckInTest', '16', 'QR scan and typed code; expired token, closed session, outsider, duplicate; signed-out scan; opening, rotation, the polling window, and closing marking the no-shows absent'],
    ['CatalogueTest', '19', 'Course and section rules; registry and student enrolment; dropping rather than deleting; timetable slots; generating a term of meetings'],
    ['AttendanceReportTest', '10', 'Late counts as attended, excused leaves the denominator, open sessions excluded, at-risk ordering, one query per roster'],
    ['AccessControlTest', '12', 'Sign-in, role routing, deactivated accounts, cross-portal refusals, ownership policies'],
    ['PagesRenderTest', '10', 'Every screen in all three portals, including every module screen; search filtering; CSV export'],
    ['EnumBehaviourTest', '5', 'The shared rules carried by the enumerations: what counts as attendance, which cases are open, where each role lands'],
  ], [2600, 900, 7300]),
  TableCaption('Automated test suite'),
  Code([
    'PS C:\Users\Rakze\Desktop\GroupAssignment> php artisan test',
    '',
    '   PASS  Tests\Unit\EnumBehaviourTest',
    '   PASS  Tests\Feature\AccessControlTest',
    '   PASS  Tests\Feature\AttendanceReportTest',
    '   PASS  Tests\Feature\CatalogueTest',
    '   PASS  Tests\Feature\CheckInTest',
    '   PASS  Tests\Feature\ModuleIntegrationTest',
    '   PASS  Tests\Feature\ModuleWorkflowTest',
    '   PASS  Tests\Feature\PagesRenderTest',
    '   PASS  Tests\Feature\PhotoUploadTest',
    '   PASS  Tests\Feature\StudentProfileTest',
    '',
    '  Tests:    129 passed (390 assertions)',
    '  Duration: 3.65s',
  ]),
  FigCaption('Automated test run.'),

  H2('5.3 Test Cases'),
  P('The table below records the eighty principal test cases, each traced to the requirement it verifies. All were executed against the finished system.'),
  Tbl(
    ['ID', 'Req.', 'Test case', 'Input', 'Expected result', 'Result'],
    require('./s5cases.js'),
    [700, 750, 2300, 2300, 3750, 1000],
  ),
  TableCaption('Test cases, traced to requirements'),

  H2('5.4 Defects Found and Fixed'),
  P('Five defects surfaced during development. The first two were caught by the database and the environment rather than the application. The remaining three were found by walking the system in a browser, which is the only place any of them could have shown up: two were defects in the demonstration data, and the last was a timing fault that existed only while the system was left running.'),

  H3('Defect 1: A constraint doing its job'),
  Tbl(['Aspect', 'Detail'], [
    ['Symptom', 'A test that gave ten students attendance in the same section failed with a unique constraint violation on the attendance sessions table.'],
    ['Cause', 'The test helper created a fresh session for each student, so ten sessions were created for the same section at the same date and time. The constraint UNIQUE (class_section_id, session_date, start_time) correctly refused the second.'],
    ['Assessment', 'This was not a defect in the system. It was the schema preventing something that cannot happen in reality: a class cannot meet twice at the same moment. The test was wrong, not the constraint.'],
    ['Fix', 'The helper now creates the sessions once and marks every student against them, which is also what the real system does.'],
  ], [1800, 9000]),
  TableCaption('Defect 1'),

  H3('Defect 2: A test that could not run on the target machine'),
  Tbl(['Aspect', 'Detail'], [
    ['Symptom', 'Every photograph test failed with the message "GD extension is not installed".'],
    ['Cause', 'The framework helper that fabricates a fake image generates real pixel data and therefore requires the GD extension. The development and target machines have neither GD nor Imagick.'],
    ['Assessment', 'The application code was correct; the tests had a dependency the deployment target does not satisfy. Requiring GD purely for testing would have contradicted NFR-10.'],
    ['Fix', 'The fake uploads are now constructed from an explicit MIME type rather than generated pixels. The tests exercise the same validation path and run anywhere.'],
  ], [1800, 9000]),
  TableCaption('Defect 2'),
  P('A third issue was anticipated rather than discovered: MySQL limits an identifier to 64 characters, which the generated name for a three-column unique index exceeds. Those constraints were given explicit short names from the outset.'),

  H3('Defect 3: A download that led nowhere'),
  Tbl(['Aspect', 'Detail'], [
    ['Symptom', 'On the seeded database, every Download link on the assignment screen returned a not-found page, although the submission was listed with a filename and a status.'],
    ['Cause', 'The seeder wrote submission rows carrying a file path but never wrote a file at that path. The controller checks that the file exists before streaming it, so it correctly refused. Nothing in the automated suite caught this, because those tests upload a real file first.'],
    ['Assessment', 'The application was right and the demonstration data was wrong, but the effect on a marker following the demonstration path is the same as a defect.'],
    ['Fix', 'The seeder now writes a genuine one-page PDF for every seeded submission, assembled in a few lines of PHP so that no additional library is required. Downloads on seeded rows return a real document.'],
  ], [1800, 9000]),
  TableCaption('Defect 3'),

  H3('Defect 4: Forty-five students, two surnames'),
  Tbl(['Aspect', 'Detail'], [
    ['Symptom', 'A class roster of twenty-three students showed the same family name against every one of them, and two students on the same roster shared a full name.'],
    ['Cause', 'Names were composed by stepping through a list of fifteen given names and a list of ten family names. The family-name step multiplied the counter by five against a list of ten, and five and ten share a factor, so the expression could only ever produce two of the ten positions.'],
    ['Assessment', 'A demonstration defect rather than a functional one, but a costly one: every screenshot in this report is taken from the seeded database, and a roster of identical names makes the system look as though it cannot tell students apart.'],
    ['Fix', 'The multiplier was changed to one that shares no factor with the list length, with a further term derived from the counter so the two lists no longer share a period. The cohort now holds forty-five distinct names and a single roster shows all ten family names.'],
  ], [1800, 9000]),
  TableCaption('Defect 4'),

  H3('Defect 5: A code that expired before it was replaced'),
  Tbl(['Aspect', 'Detail'], [
    ['Symptom', 'During the walkthrough a QR code shown on the kiosk scanned cleanly, but the check-in behind it was refused as expired, while the page still displayed the code as current.'],
    ['Cause', 'The kiosk polls every forty-five seconds and a token lives sixty. The refresh endpoint minted a new token only when five seconds or fewer remained. At the forty-five second poll fifteen seconds remained, so nothing was replaced; the token expired at sixty and was not replaced until ninety. For thirty seconds of every cycle, the code on the projector was dead.'],
    ['Assessment', 'A genuine defect, and one no unit test would have found: each piece behaved correctly on its own, and the fault lived in the relationship between two configured intervals. It surfaced only when the feature was left running for longer than a single request.'],
    ['Fix', 'The condition is now expressed against the polling interval rather than a fixed margin: a token is replaced whenever it would not survive until the next poll. Two regression tests hold it, one asserting a token near the polling boundary is replaced and one asserting a token with plenty of life left is left alone.'],
  ], [1800, 9000]),
  TableCaption('Defect 5'),

  H2('5.5 User Acceptance Walkthrough'),
  P('The complete demonstration path was executed in a browser against the seeded database.'),
  Num('Signed in as the registry. The dashboard reported 45 active students across 6 programmes, 5 students below the attendance requirement and 8 open complaints, each figure from a different subsystem.'),
  Num('Opened a profile showing the personal record, address and guardian alongside 88.9 per cent attendance, 8 of 12 assignments submitted and 1 open complaint.'),
  Num('Created a profile with two addresses and a guardian, confirming blank repeat rows are discarded.'),
  Num('Searched by student number and filtered by programme; the list narrowed correctly.'),
  Num('Opened the completeness report and downloaded the by-programme report as CSV.'),
  Num('Signed in as a faculty member: only students in their own sections were listed, and editing the address bar to reach another produced the access-denied page.'),
  Num('As the same faculty member, opened a register, changed one student from absent to excused and saved it. The list returned "Register saved for 23 student(s)", and the database held twenty-three rows for that session with one row for that student, confirming an update rather than a duplicate.'),
  Num('Opened a scheduled meeting for check-in. The kiosk displayed a QR code, the six-character code, a countdown and a live tally reading zero present against a roster of twenty-three.'),
  Num('Signed in as a student in a separate session and typed the six-character code. The reply was "Checked in. You are marked present," and the kiosk tally moved to one without the page reloading.'),
  Num('As a second student, opened the QR link directly, which is what a phone camera does. That student was also recorded present, with the source stored as a scan rather than a typed code.'),
  Num('Repeated the same scan. It was refused with "You have already been marked for this session," and no second record was written.'),
  Num('Closed the session. The system reported "Session closed. 21 student(s) who never checked in were marked absent," leaving twenty-three records: two present and twenty-one absent, with both check-ins untouched. The token and short code were cleared, so the code on the projector stopped working.'),
  Num('Opened the at-risk report as the registry. Twenty-one enrolments were listed below seventy-five per cent, worst first, and the CSV download carried the same rows.'),
  Num('Opened a class section as the registry and confirmed its weekly timetable, its roster, and the counts of meetings and assignments recorded against it.'),
  Num('Opened an assignment and confirmed the submission list, the on-time and late counts, and that a Download link returned a PDF under the submitting student\u2019s own filename.'),
  Num('Signed in as a student and confirmed only their own record was visible.'),
  Num('As the student, opened My assignments and confirmed each class showed its deadline, the submitted file and the option to replace it.'),
  Num('As the student, raised a complaint about a timetable clash. The case was accepted and issued the reference CMP-00013.'),
  Num('Signed back in as the registry, opened CMP-00013, recorded a response and set the case to resolved. The case detail then showed the handling officer and the resolution date.'),
  Num('Returned to the student portal and confirmed the registry response was visible on the student\u2019s own copy of the case.'),
  P('Observed behaviour matched the expected result in every case.'),
];
